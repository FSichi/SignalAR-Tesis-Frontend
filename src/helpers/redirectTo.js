
export const backtoLogin = (url) => {
    window.location.href = url
}
export const goToDashboard = (url) => {
    window.location.href = url
}

export const goToURL = (url) => {
    window.location.href = url
}